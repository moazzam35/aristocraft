/**
 * FloatingHeader — the public-facing floating navigation bar.
 *
 * The actual implementation lives in @/components/ui/SidebarNav (legacy filename).
 * This re-export provides a clearer import path:
 *
 *   import FloatingHeader from "@/components/layout/FloatingHeader";
 */
export { default } from "@/components/ui/SidebarNav";
